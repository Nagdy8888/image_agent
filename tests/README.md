# Tests

Test suite and notebooks for the image tagging agent.

| Item | Purpose |
|------|---------|
| `notebooks/` | Jupyter notebooks (e.g. test_agent.ipynb) for manual or exploratory runs. |
| (unit/integration) | Add pytest or similar under `tests/` as needed. |

Run notebooks: `jupyter notebook` or open in VS Code/Cursor.
