# schemas

State and data models for the image tagging pipeline.

| File | Purpose |
|------|---------|
| `states.py` | LangGraph state (TypedDict) |
| `tags.py` | Pydantic models (TagResult, ValidatedTag, TagRecord, etc.) |
