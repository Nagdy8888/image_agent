# supabase

Supabase (PostgreSQL) integration for storing and retrieving tag records.

| File | Purpose |
|------|---------|
| `client.py` | build_search_index, upsert_tag_record, get_tag_record, list_tag_images, search_by_tags |
| `settings.py` | DATABASE_URL / DATABASE_URI, SUPABASE_ENABLED |
