# src

Backend application and image tagging agent code.

| File / Folder | Purpose |
|---------------|---------|
| `server.py` | FastAPI application server |
| `services/` | Shared services (Supabase client, settings) |
| `image_tagging/` | LangGraph agent (graph, nodes, prompts, schemas) |
